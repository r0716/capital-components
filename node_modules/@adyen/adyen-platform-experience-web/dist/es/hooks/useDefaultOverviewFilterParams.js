import { useState as m, useRef as l, useCallback as f, useEffect as u } from "../external/preact/hooks/dist/hooks.module.js";
import { FilterParam as t } from "../components/types.js";
import { getTimeRangeSelectionDefaultPresetOptions as T } from "../components/internal/DatePicker/components/TimeRangeSelector/useTimeRangeSelection.js";
import { DEFAULT_TRANSACTIONS_OVERVIEW_MULTI_SELECTION_FILTER_PARAMS as c } from "../components/external/TransactionsOverview/components/MultiSelectionFilter/useMultiSelectionFilter/constants.js";
const A = (r) => {
  const e = T(), a = "rangePreset.last30Days", { from: s, to: o } = e[a];
  return { defaultFilterParams: {
    ...r === "transactions" && {
      ...c,
      [t.MIN_AMOUNT]: void 0,
      [t.MAX_AMOUNT]: void 0
    },
    [t.BALANCE_ACCOUNT]: void 0,
    [t.CREATED_SINCE]: new Date(s).toISOString(),
    [t.CREATED_UNTIL]: new Date(o).toISOString()
  }, defaultTimeRange: a, timeRangeOptions: e };
}, E = (r, e) => {
  const [a, s] = m(Date.now()), o = A(r), i = l(o), n = f(() => s(Date.now()), [s]);
  return u(() => {
    n();
  }, [e, n]), { defaultParams: i, nowTimestamp: a, refreshNowTimestamp: n };
}, S = E;
export {
  S as default
};
