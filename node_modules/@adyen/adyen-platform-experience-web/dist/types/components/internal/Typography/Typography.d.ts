import { TypographyElement, TypographyVariant } from './types';
import { PropsWithChildren } from 'preact/compat';
import './Typography.scss';
import { ComponentChild } from 'preact';
interface TypographyProps {
    el?: TypographyElement;
    stronger?: boolean;
    strongest?: boolean;
    variant: TypographyVariant;
    medium?: boolean;
    large?: boolean;
    wide?: boolean;
    children: ComponentChild;
    className?: string;
    testId?: string;
}
declare function Typography({ el, className, stronger, strongest, variant, medium, large, testId, wide, children }: PropsWithChildren<TypographyProps>): import("preact").JSX.Element;
declare const _default: typeof Typography;
export default _default;
//# sourceMappingURL=Typography.d.ts.map